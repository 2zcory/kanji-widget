<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingTop="12dp"
    android:paddingBottom="12dp"
    android:background="?android:attr/selectableItemBackground">

    <TextView
        android:id="@+id/tvInsightKanji"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:background="@drawable/bg_widget_pill_primary"
        android:gravity="center"
        android:textColor="#102030"
        android:textSize="22sp"
        android:textStyle="bold"
        tools:text="鬱" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="14dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvInsightName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#102030"
            android:textSize="15sp"
            android:textStyle="bold"
            tools:text="Depression" />

        <TextView
            android:id="@+id/tvInsightReason"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#627488"
            android:textSize="12sp"
            tools:text="Viewed 15 times, studied less than 1m" />
    </LinearLayout>

    <Button
        android:id="@+id/btnInsightReview"
        android:layout_width="wrap_content"
        android:layout_height="36dp"
        android:background="@drawable/bg_widget_pill_primary"
        android:text="@string/dashboard_action_review"
        android:textColor="#1272D8"
        android:textSize="12sp"
        android:textStyle="bold"
        android:textAllCaps="false"
        android:paddingStart="12dp"
        android:paddingEnd="12dp" />

</LinearLayout>
